# Telegram Bot
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_ALLOWED_USERS=your-telegram-user-id

# Gateway
GATEWAY_PORT=4300
MCP_PORT=4301

# Database
DATABASE_PATH=./data/assistant.db

# Output directory for generated files (reports, exports, etc.)
OUTPUT_DIR=/path/to/your/output/folder
# Optional: short label for display (defaults to last 2 path segments of OUTPUT_DIR)
# OUTPUT_DIR_LABEL=OneDrive/Desktop/AI-Assistant

# --- Docker-specific ---
# When running in Docker, DATABASE_PATH should point to the mounted volume:
#   DATABASE_PATH=/app/data/assistant.db
# Claude CLI credentials are persisted via the claude-config volume.
# After first start, authenticate with:
#   docker exec -it <container> claude login
